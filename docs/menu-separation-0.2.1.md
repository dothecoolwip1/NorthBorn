# Northborn 0.2.1

- Keep one real navigation hamburger for all signed-in roles.
- Move test account role switching into a separate test-only role control.
- Hide the legacy manager mobile hamburger so it no longer duplicates global navigation.
- Keep Settings, install controls, update controls and version in the real navigation menu.
- Test role switching is only shown for the dedicated manager/operator/client test accounts and does not render for normal accounts.
